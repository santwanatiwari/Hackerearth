#include<stdio.h>
int main()
{
	char word[50];
	int x=0,y=0;
	scanf("%s",&word);
	for(int i=0;word[i]!='\0';i++){
	if(word[i]=='z')
	x++;
	else if(word[i]=='o')
	y++;
	}
	if(2*x==y)
	printf("Yes");
	else
	printf("No");
}
