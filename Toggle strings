#include<stdio.h>
#include<string.h>
int main()
{
	char s[100];
	int i;
	scanf("%s",s);
	while(s[i]!='\0')
	{
		if(s[i]>='a'&& s[i]<='z')
		s[i]=s[i]-32;
		else
		s[i]=s[i]+32;
		i++;
	}
	printf("%s",s);
}
